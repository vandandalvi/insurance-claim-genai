services:
  # Main Backend Service
  - type: web
    name: claimsense-backend
    env: python
    plan: free
    rootDir: genai-backend
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: "3.11.7"

  # Chatbot Service
  - type: web
    name: claimsense-chatbot
    env: python
    plan: free
    rootDir: genai-backend
    buildCommand: pip install -r requirements.txt
    startCommand: python appi.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: "3.11.7"

  # Frontend Static Site
  - type: web
    name: claimsense-frontend
    env: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://claimsense-backend-71pr.onrender.com
      - key: VITE_CHAT_URL
        value: https://claimsense-chatbot-71pr.onrender.com 